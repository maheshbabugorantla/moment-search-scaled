# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.python-version

# Environment / secrets
.env
.env.*
!.env.example

# Runtime data (videos, extracted frames, qdrant storage)
data/
backend/data/
qdrant_storage/

# Model caches
.cache/
*.pt
*.onnx

# Node / frontend
node_modules/
dist/

# Benchmark — upstream ignores this wholesale; Assignment 3 requires the SLA
# gate to be committed (it is the grading gate and a CI check). Track the
# harness and its fixtures; keep generated results out.
benchmark/*
!benchmark/bench.py
!benchmark/sla.json
!benchmark/queries.jsonl
!benchmark/README.md

# Internal design notes (not published)
MULTIMODAL_RETRIEVAL.md

# OS / editor
.DS_Store
.idea/
.vscode/
*.log
