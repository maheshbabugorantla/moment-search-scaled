[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    mutating: writes to the manifest (creates and deletes a throwaway row).
        Deselect with -m "not mutating" for a strictly read-only run.
